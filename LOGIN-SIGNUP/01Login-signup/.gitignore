# Ignore everything
*

# But NOT these files/folders (whitelist them)
!src/
!index.html

# Inside src folder, still ignore unwanted files (optional)
src/*.test.js
src/setupTests.js

# Also ignore common unnecessary files
node_modules/
dist/
build/
.vscode/
.env
.DS_Store
*.log
